// import { useAuth } from '../App';


const TasksPage = () => {
  // const { user, token } = useAuth();
  // user;
  // token;

  return (
    <div>
      <h1>Tasks Page</h1>
      <p>Welcome, !</p>
    </div>
  );
};

export default TasksPage;

